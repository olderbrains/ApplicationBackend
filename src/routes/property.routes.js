const express = require('express');
const router = express.Router();
const propertyController = require('../controllers/property.controller');

// Future: router.post('/add', propertyController.addProperty);

module.exports = router;
